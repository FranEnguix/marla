"""First-class Optuna hyperparameter-study support for MARLA PPO tuning.

See ``research/OPTUNA_STUDY.md`` for the full methodology (search space,
sampler, objective, two-seed evaluation, collapse constraint, finalist/
holdout procedure). CLI entry point: ``marla optimize <study-config.yaml>``.
"""
